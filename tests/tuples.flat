int main () := {

    [int, bool, [str, int]] tuple := [5, true, ["something", 0]]; 
    [str, int] pair := tuple^2;

    int last := pair^1;

    str last2 := tuple^2^0;

    str new := [5, "str"]^1;
    
    return 0;
}